# def generate_numbers(n):  #Doesn't store in memory this makes it memory effiecient 
#     for i in range(n):    #whereas other for loops store in memory
#         yield i
    
# numbers = generate_numbers(1000000)
# print(sum(numbers))